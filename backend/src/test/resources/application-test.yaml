spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:

  flyway:
    enabled: false

  jackson:
    property-naming-strategy: LOWER_CAMEL_CASE

jwt:
  secret: test-jwt-secret-key-that-is-at-least-32-bytes-long-for-testing

logging:
  level:
    root: WARN
    com.cwj.onlineorder: DEBUG
